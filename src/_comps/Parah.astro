---
import { cn } from "@/_utils/tailwind.utils";
import { cva } from "class-variance-authority";

interface Props {
    className?: string;
}

const { className = "" } = Astro.props;

const parah = cva(["text-xs"]);
---

<p class={cn(parah(), className)}>
    <slot />
</p>
