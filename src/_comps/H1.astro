---
import { cn } from "@/_utils/tailwind.utils";
import { cva } from "class-variance-authority";

interface Props {
    className?: string;
}

const { className = "" } = Astro.props;

const h1 = cva(["font-bold text-sm"]);
---

<h1 class={cn(h1(), className)}>
    <slot />
</h1>
